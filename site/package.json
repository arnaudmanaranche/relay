{
  "name": "relay-site",
  "private": true,
  "type": "module",
  "description": "Source for the Relay documentation site. Builds into ../docs, which GitHub Pages serves — never edit ../docs by hand.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^5.4.11"
  }
}
