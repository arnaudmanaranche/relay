{
  "name": "relay-studio",
  "private": true,
  "type": "module",
  "description": "Local editor for pipeline role prompts and their attached skills. Reads and writes files directly under the project root Studio is launched from (must have run /relay:setup there first) \u2014 dev-only tool, never published.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^5.4.11"
  }
}
